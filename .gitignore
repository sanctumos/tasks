# Local reference clones / scratch
tmp/

# SQLite DBs (production DB should live on server; don't commit)
db/*.db
db/*.sqlite
db/*.sqlite3

# Local secrets (persist on server under db/, but never commit)
db/api_key.txt
db/bootstrap_admin_password.txt
db/otto_agent_api_key.env
db/secrets.php
public/includes/secrets.php

# Python virtual environments and build artifacts
.venv-ci/
python/venv/
*.egg-info/
*.egg
__pycache__/
*.pyc
.pytest_cache/
.coverage

# OS / tooling
.DS_Store
Thumbs.db
*.log
*.tmp

# Playwright design-smoke screenshot output (regenerate locally)
tools/design-smoke/output/

# PHP (Composer / PHPUnit)
vendor/
.phpunit.cache/

